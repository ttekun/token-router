interface:
  display_name: "Token Router"
  short_description: "Route large files through local Ollama"
  default_prompt: "Use $token-router to analyze this large log or source file with a local Ollama line router."
