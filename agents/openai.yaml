interface:
  display_name: "Token Router"
  short_description: "Route large files and agent-context refs through local Ollama"
  default_prompt: "Use $token-router to analyze this large log, source file, or routeable agent-context reference with a local Ollama line router."
